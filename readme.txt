To run modify the application.properties file to use the proper data source.
Run TodolistbackendApplication.java to start the backend.
Go to angular-frontend on a terminal then type "ng serve" to start frontend.